name: Car Wash CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven' # This makes future builds much faster

      - name: Build with Maven
        run: |
          cd backend/car-wash-api
          chmod +x mvnw # Use the wrapper if you have it
          ./mvnw clean package -DskipTests 
          # OR if you don't have the wrapper:
          # mvn clean package -DskipTests

  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/car-wash-web/package-lock.json

      - name: Install and Build
        run: |
          cd frontend/car-wash-web
          npm install
          npm run build