// TODO: no dedicated clients endpoint exists in the backend.
// Derive from bookings or request a new endpoint.
// Stub only.
export async function fetchAllClients(): Promise<never[]> {
  return []
}
